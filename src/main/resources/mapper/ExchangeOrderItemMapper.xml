<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cityquest.mapper.ExchangeOrderItemMapper">
    
    <resultMap id="OrderItemResultMap" type="com.cityquest.entity.ExchangeOrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="points_price" property="pointsPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="subtotal_points" property="subtotalPoints"/>
        <result column="create_time" property="createTime"/>
    </resultMap>
    
    <select id="selectByOrderId" parameterType="Long" resultMap="OrderItemResultMap">
        SELECT * FROM exchange_order_item WHERE order_id = #{orderId} ORDER BY create_time ASC
    </select>
    
    <insert id="insert" parameterType="com.cityquest.entity.ExchangeOrderItem" useGeneratedKeys="false">
        INSERT INTO exchange_order_item (id, order_id, product_id, product_name, product_image, points_price, quantity, subtotal_points, create_time)
        VALUES (#{id}, #{orderId}, #{productId}, #{productName}, #{productImage}, #{pointsPrice}, #{quantity}, #{subtotalPoints}, #{createTime})
    </insert>
    
    <insert id="insertBatch">
        INSERT INTO exchange_order_item (id, order_id, product_id, product_name, product_image, points_price, quantity, subtotal_points, create_time)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.id}, #{item.orderId}, #{item.productId}, #{item.productName}, #{item.productImage}, #{item.pointsPrice}, #{item.quantity}, #{item.subtotalPoints}, #{item.createTime})
        </foreach>
    </insert>
    
    <delete id="deleteByOrderId" parameterType="Long">
        DELETE FROM exchange_order_item WHERE order_id = #{orderId}
    </delete>
</mapper>

